# EC2-1 (bench-1) 環境變數設定
# nginx + framework 服務配置

# Prisma 資料庫連接 URL (連接到 EC2-2)
DATABASE_URL="mysql://benchuser:benchpass@<EC2-2-PRIVATE-IP>:3306/benchdb"

# 個別資料庫參數 (向後兼容)
DATABASE_HOST=<EC2-2-PRIVATE-IP>
DATABASE_PORT=3306
DATABASE_NAME=benchdb
DATABASE_USER=benchuser
DATABASE_PASSWORD=benchpass

# 應用程式設定
NODE_ENV=production
PORT=3000

# 日誌設定
LOG_LEVEL=info

# PM2 設定
PM2_INSTANCES=max
PM2_MAX_MEMORY_RESTART=1G

# 範例：
# DATABASE_URL="mysql://benchuser:your-secure-password@10.0.1.100:3306/benchdb"
# DATABASE_HOST=10.0.1.100
# DATABASE_PORT=3306
# DATABASE_NAME=benchdb
# DATABASE_USER=benchuser
# DATABASE_PASSWORD=your-secure-password